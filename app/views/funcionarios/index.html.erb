<ul class="nav nav-tabs">
  <li class="active">
    <a href="#">Listagem de funcionários</a>
  </li>
</ul>
<div class="section-menu">

<table class="table table-striped table-bordered table-condensed table-hover">
  <tr>
    <th>Cod</th>
    <th>Login</th>
    <th>Nome</th>
    <th></th>
  </tr>

<% @funcionarios.each do |funcionario| %>
  <tr>
    <td><%= funcionario.id %></td>
    <td><%= funcionario.login %></td>
    <td><%= funcionario.nome %></td>
    <td>
      <div class="btn-group">
        <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
          Ações
          <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
          <!-- dropdown menu links -->
          <li><%=link_to 'Visitas', funcionario%></li>
          <li><%=link_to 'Editar', edit_funcionario_path(funcionario)%></li>
        </ul>
      </div>
      <!-- 
      <%= content_tag :i, (link_to ' Visitas', funcionario), :class => " icon-th-large" %> 
      <%= content_tag :i, (link_to ' Editar', edit_funcionario_path(funcionario)), :class => "icon-remove" %> 
       -->
      <!--  
      <%= content_tag :i, (link_to ' Desligar', funcionario, method: :delete, data: { confirm: 'Are you sure?' }), :class => "icon-remove-sign" %> 
       -->
  </td>
  </tr>
<% end %>
</table>
</div>

