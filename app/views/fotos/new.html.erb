<%= form_for @foto, :html => {:multipart => true} do |f| %>
  <p>
    <label>My Avatar</label>
    <%= f.file_field :avatar %>
    <%= f.hidden_field :avatar_cache %>
  </p>
  <div class="actions">
    <%= f.submit%>
  </div>
<% end %>